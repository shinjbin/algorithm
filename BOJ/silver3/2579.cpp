#include <iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int n;
    cin >> n;

    int stairs[n];

    for (int i = 0; i < n; i++) {
        cin >> stairs[i];
    }
    
    
    return 0;
}